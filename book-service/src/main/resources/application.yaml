#spring:
#  application:
#    name: BOOK-SERVICE
#  config:
#    import: "configserver:http://config.platform:8088"
#

server:
  port: 0

spring:
  application:
    name: book-service
  config:
    import: "configserver:http://config.platform:8088"